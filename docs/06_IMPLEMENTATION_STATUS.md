# GloryProtect — статус реализации

## Целевая версия

Godot **4.6.2 stable**, строго типизированный GDScript.

## Текущая версия

**Prototype 0.8 — физическое разделение, очереди на тросах и взаимное блокирование.**

---

## 1. Забег

Реализованы:

- безопасная стартовая задержка;
- активная игра;
- полная ручная пауза;
- состояние выбора карточек;
- завершение забега и причина поражения;
- поражение при 0% любой секции;
- немедленное поражение при отсутствии живых защитников.

Ожидающие замены не отменяют поражение при нуле живых защитников.

---

## 2. Платформа, ветер и якоря

- горизонтальное движение через силу;
- рулевой ввод только при активном рулевом;
- ветер силы 1–3;
- четыре якоря;
- очередь установки;
- конечная длина троса в обе стороны;
- полная фиксация;
- перегрузка одиночного троса;
- срыв и возврат;
- привязка якоря к конкретному шару;
- публичные снимки маршрутов для BoardingDomain.

---

## 3. Пять шаров и секций

- пять фиксированных шаров;
- пять независимых секций по 100%;
- узкая контактная зона;
- непрерывная зарядка связанной секции;
- порог указателя 50%;
- критический порог 25%;
- визуальное состояние шаров;
- энергетический луч;
- поражение при разрушении секции.

---

## 4. Экипаж и роли

Композиция:

```text
CrewManager
CrewRoleManager
CrewReplacementController
CrewCombatCoordinator

Defender
├── HealthComponent
├── DefenderMovement
├── MeleeAttackComponent
├── DefenderCombatController
└── DefenderVisual
```

Реализованы:

- рулевой и два якорщика в начале;
- физическое перемещение между постами;
- резервирование рабочих мест;
- ожидание рулевого ввода, операции якоря и взмаха;
- очистка ближайшей боевой зоны перед переназначением;
- стабильные слоты `defender_id`;
- независимые кулдауны замен;
- появление замены с полным здоровьем;
- роль `FREE_FIGHTER` после появления;
- выбор ближайшей свободной позиции, если дверь занята врагом.

### Движение и блокирование

`DefenderMovement` теперь:

- сохраняет конечную точку назначения при остановке на бой;
- может приостанавливаться и возобновляться;
- не проходит сквозь врагов;
- продолжает игнорировать других защитников.

Защитник, идущий к новому посту:

1. останавливается перед блокирующим врагом;
2. завершает бой;
3. после освобождения прохода продолжает прежний маршрут.

---

## 5. Свободный боец

Свободный защитник:

1. выбирает ближайшего живого врага на платформе;
2. движется к нему;
3. останавливается на допустимой дистанции;
4. завершает неделимый взмах;
5. после смерти цели выбирает следующую.

При ожидающем переназначении дальние цели больше не преследуются.

---

## 6. Ближний бой

`MeleeAttackComponent` использует фазы:

```text
READY
WINDUP
COOLDOWN
```

Правила:

- цель фиксируется при начале взмаха;
- атака не перенаправляется;
- погибшая цель превращает удар в промах;
- обычный удар наносит 1 урон;
- защитник атакует быстрее базового врага;
- несколько атак могут независимо быть направлены на одну цель.

---

## 7. Физический абордаж

Компоненты:

```text
AnchorPathRegistry
BoardingSpawnDirector
BoardingEnemyRegistry
BoardingMovementResolver
BoardingEnemyContainer

BoardingEnemy
├── HealthComponent
├── MeleeAttackComponent
├── BoardingEnemyController
└── BoardingEnemyVisual
```

### BoardingMovementResolver

Добавлен единый сервис расчёта допустимого движения.

Он не хранит координаты и не двигает персонажей самостоятельно. Он возвращает контроллеру допустимую координату или прогресс.

Реализованы:

- минимальная дистанция наземных врагов;
- безопасные точки наземного спавна;
- очередь перед входом на трос;
- минимальная дистанция между врагами на одном тросе;
- ожидание возле верхнего конца троса, если выход занят;
- физическое разделение врагов на платформе;
- блокирование между врагом и защитником;
- поиск свободной позиции для появляющейся замены.

### Правила движения

- враги не проходят сквозь врагов;
- враги и защитники не проходят друг сквозь друга;
- защитники могут проходить сквозь защитников;
- задний враг ждёт переднего;
- несколько врагов могут одновременно находиться на одном тросе с минимальной дистанцией;
- если верх троса занят, враг остаётся в состоянии `CLIMBING`;
- снятие якоря убивает всех врагов, которые ещё находятся на тросе;
- враг, вышедший на платформу, переживает снятие.

### Текущие дистанции

```text
земля: 28 px
трос: 30 px
платформа между врагами: 28 px
враг — защитник: сумма радиусов персонажей
```

Все значения читаются из:

```text
resources/balance/boarding_balance.tres
resources/balance/crew_balance.tres
```

---

## 8. HUD и временный визуал

HUD показывает:

- состояние забега;
- ветер и движение;
- якоря;
- роли экипажа;
- независимые таймеры замен;
- врагов на земле, тросах и платформе;
- активный контакт;
- пять процентов щита;
- направления к секциям 50% и ниже;
- причину поражения.

На платформе отображается временная дверь замены.

---

## 9. Управление

### Платформа и якоря

| Действие | Клавиша |
|---|---|
| Рулевое усилие | `←` / `→` |
| Якоря | `1–4` |
| Снять все якоря | `R` |
| Пауза | `Esc` |

### Экипаж

| Действие | Клавиша |
|---|---|
| Выбрать защитника | `5`, `6`, `7` |
| Рулевой | `D` |
| Левый якорщик | `Z` |
| Правый якорщик | `X` |
| Свободный защитник | `C` |

### Щит

| Действие | Клавиша |
|---|---|
| Выбрать секцию | `F1–F5` |
| Тестовый урон | `Space` |

---

## 10. Тесты

### Unit

```text
tests/unit/anchor_constraint_scenarios.gd
tests/unit/platform_resource_scenarios.gd
tests/unit/melee_attack_scenarios.gd
```

### Integration

```text
tests/integration/crew_role_scenarios.gd
tests/integration/shield_orb_scenarios.gd
tests/integration/boarding_enemy_scenarios.gd
tests/integration/crew_replacement_scenarios.gd
tests/integration/free_fighter_pursuit_scenarios.gd
tests/integration/boarding_separation_scenarios.gd
```

Новый сценарий проверяет:

- разделение двух врагов на платформе;
- невозможность пройти сквозь защитника;
- сохранение дистанции между защитником и врагом;
- одновременный подъём двух врагов по одному тросу;
- минимальную дистанцию между ними.

Команды:

```bash
godot --headless --path . --script res://tests/unit/anchor_constraint_scenarios.gd
godot --headless --path . --script res://tests/unit/platform_resource_scenarios.gd
godot --headless --path . --script res://tests/unit/melee_attack_scenarios.gd
godot --headless --path . --script res://tests/integration/crew_role_scenarios.gd
godot --headless --path . --script res://tests/integration/shield_orb_scenarios.gd
godot --headless --path . --script res://tests/integration/boarding_enemy_scenarios.gd
godot --headless --path . --script res://tests/integration/crew_replacement_scenarios.gd
godot --headless --path . --script res://tests/integration/free_fighter_pursuit_scenarios.gd
godot --headless --path . --script res://tests/integration/boarding_separation_scenarios.gd
python tools/check_file_sizes.py
```

В среде помощника нет исполняемого файла Godot, поэтому headless-тесты здесь не запускались.

---

## 11. Временные ограничения

Пока не реализованы:

- прыжок врага через защитника;
- проверка свободного места за спиной перед прыжком;
- увеличение максимального экипажа улучшением;
- монеты за физические убийства;
- рост наземного лимита от сложности;
- стратегические волны и полноценная миникарта;
- лекарь и турели;
- финальное контекстное меню ролей;
- финальные анимации и звук.

---

## 12. Следующая итерация

1. прыжок врага через защитника;
2. условие блокировки другим врагом;
3. проверка свободного места за защитником;
4. немедленная переоценка ближайшей цели после приземления;
5. монеты за физические убийства;
6. рост наземного лимита от сложности;
7. начало стратегических волн.
