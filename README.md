# GloryProtect

GloryProtect — survival roguelike с видом сбоку, в котором игрок управляет магически парящей платформой, поддерживает пять секций глобального щита и защищает экипаж от физического абордажа.

## Движок

Godot **4.6.2 stable**, строго типизированный GDScript.

## Обязательные документы

- [Правила проекта](PROJECT_RULES.md)
- [Архитектура проекта](ARCHITECTURE.md)
- [Инструкции для coding agents](AGENTS.md)
- [Правила прочности тросов](docs/rules/08_ROPE_DURABILITY.md)
- [Правила подрывника троса](docs/rules/09_ROPE_SABOTEUR.md)

Основные ограничения:

- максимум 600 строк на поддерживаемый файл;
- каждое изменяемое состояние имеет одного владельца;
- баланс находится в типизированных ресурсах;
- UI не владеет игровой логикой.

## Текущая версия

**Prototype 2.1 foundation** содержит:

- горизонтальную платформу и ветер силы 1–3;
- четыре якоря с конечной длиной троса;
- независимую runtime-прочность каждого из четырёх тросов;
- шкалы прочности и критическое предупреждение на установленных тросах;
- физический обрыв, закрытие boarding-пути и автоматический возврат якоря;
- публичный API урона тросу, snapshots и события разрушения;
- подрывника, который атакует установленный трос с земли;
- пять наземных шаров и секций щита;
- три защитника, роли, замены и ближний бой;
- физический абордаж через якоря;
- data-driven каталог физических врагов;
- подключаемые специальные поведения врагов;
- состав абордажа, меняющийся с ростом сложности;
- стратегические волны и миникарту;
- экономику, статистику и итоговый экран;
- медицинский пост и несколько независимых турелей;
- мышиную панель выбора экипажа и назначения ролей;
- две одинаковые карточки-заглушки без эффектов.

При нулевой прочности путь закрывается синхронно, враги на разрушенном тросе погибают через общий `anchor_path_closed` flow, а уже поднявшиеся на платформу продолжают бой. Якорь автоматически возвращается и после новой установки получает полный трос.

## Прочность тросов

Максимальная прочность задаётся в `AnchorBalance`. Текущее значение хранится отдельно у каждого `AnchorRuntime`, а изменяет его только `AnchorRopeDurability`.

Внешние системы используют `AnchorSystem`:

```text
apply_rope_damage(anchor_id, amount, source)
get_rope_snapshot(anchor_id)
get_all_rope_snapshots()
get_anchor_state(anchor_id)
```

`AnchorBreakRecoveryController` отвечает только за физические последствия достижения нуля: закрывает путь, удаляет врагов на конкретном тросе, сбрасывает перегрузку через переход в `RETURNING` и пересчитывает ограничения платформы.

Новая успешная установка полностью восстанавливает прочность соответствующего троса. Повреждение прочности не заменяет и не изменяет механику перегрузки ветром.

## Типы физических врагов

Обычные абордажники используют общую машину состояний:

```text
земля → выбранный якорь → подъём → платформа → ближний бой
```

Специальные враги подключают отдельный `EnemyBehaviorComponent`. Их состояние, доступность для турелей и принадлежность к наземной, тросовой или бортовой группе определяет behavior, а не проверка конкретного ID.

### Базовый абордажник

```text
Здоровье: 1
Скорость по земле: 125
Скорость подъёма: 105
Скорость на платформе: 90
Радиус тела: 12
Появляется: с начала забега
```

### Быстрый абордажник

```text
Здоровье: 1
Скорость по земле: 175
Скорость подъёма: 150
Скорость на платформе: 135
Радиус тела: 10
Более короткая подготовка атаки
Появляется: с 15% общей сложности
```

### Подрывник троса

```text
Здоровье: 1
Скорость по земле: 155
Урон тросу: 35
Подготовка: 1.6 секунды
Появляется: с 25% общей сложности
```

Подрывник выбирает ближайший работоспособный установленный трос, фиксирует цель и бежит к наземной точке крепления. Он не поднимается на платформу и не атакует защитников. После подготовки наносит урон только выбранному тросу через публичный API и погибает без награды. Убийство подрывника игроком до взрыва даёт обычную награду.

### Тяжёлый абордажник

```text
Здоровье: 3
Скорость по земле: 85
Скорость подъёма: 70
Скорость на платформе: 62
Радиус тела: 16
Более медленная атака
Появляется: с 45% общей сложности
```

В начале автоматический спавн может выбрать только базовый тип. По мере сложности открываются быстрый враг, подрывник и тяжёлый враг. Выбор выполняет `BoardingEnemyCatalog` без условных веток по `archetype_id` в общей машине движения.

Самоподрыв имеет причину смерти `rope_sabotage` и не выдаёт монеты. Стратегические враги миникарты также монет не дают.

## Данные врагов

```text
scripts/boarding/boarding_enemy_archetype.gd
scripts/boarding/boarding_enemy_catalog.gd
scripts/boarding/enemy_behavior_component.gd
scripts/boarding/rope_saboteur_behavior.gd
resources/enemies/boarding_basic.tres
resources/enemies/boarding_runner.tres
resources/enemies/boarding_rope_saboteur.tres
resources/enemies/boarding_brute.tres
resources/enemies/boarding_enemy_catalog.tres
```

Архетип задаёт:

- стабильный `archetype_id`;
- отображаемое имя;
- здоровье и радиус тела;
- скорости и обычные параметры атаки;
- диагностические цвета;
- порог открытия и вес спавна;
- необязательную behavior-сцену для специального типа.

`RopeSaboteurArchetype` дополнительно задаёт урон тросу и длительность подготовки. `BoardingBalance` продолжает владеть общими правилами спавна и физического разделения.

## Физическое разделение

Расстояние между двумя врагами вычисляется как максимум из:

```text
глобальный минимальный интервал
сумма радиусов двух конкретных архетипов
```

Правило применяется на земле, тросе, платформе и при резервировании свободной точки. Подрывник участвует в обычном наземном разделении, включая состояние подготовки возле троса.

## Диагностический внешний вид

Финальные ассеты ещё не подключены. Временная векторная графика различает обычные типы цветом и размером. Подрывник дополнительно показывает фитиль и кольцо прогресса во время подготовки.

Установленный трос показывает процент прочности и компактную шкалу. Повреждённый трос становится оранжевым, критический — красным и мигающим. Фаза мигания замораживается вместе с симуляцией.

Визуальный слой не выбирает цель и не наносит урон.

## Интерфейс экипажа

Защитника можно выбрать кликом по персонажу, портретом или клавишами `5`, `6`, `7`. Панель показывает здоровье, текущую и целевую роль, занятость постов, медицинский пост и каждую конкретную турель.

Во время карточек панель скрывается. Во время ручной паузы она остаётся видимой, но команды отключены. Диагностическая телеметрия переключается `F10`.

## Карточки

Стоимость остаётся:

```text
5, 10, 15, …, 100, 200, 400, 800
```

Обе карточки пока одинаковы и не применяют улучшение. Переход на новую систему трёх карточек реализуется отдельными issues upgrade foundation.

## Управление прототипом

Основное управление экипажем выполняется мышью.

Дополнительные клавиши:

- `←` / `→` — рулевое усилие;
- `1–4` — установить или снять якорь;
- `R` — снять все якоря;
- `Esc` — пауза;
- `5`, `6`, `7` — выбрать защитника;
- `D`, `Z`, `X`, `C`, `H` — debug-назначения ролей;
- `,` / `.` — выбрать клетку;
- `B`, `M`, `Delete` — медицинский пост;
- `T`, `G`, `K`, `V`, `J`, `Backspace` — debug-команды турелей;
- `F10` — диагностическая панель;
- `Enter` — новый забег после поражения.

## Основные тесты

```bash
godot --headless --path . --script res://tests/unit/anchor_rope_durability_scenarios.gd
godot --headless --path . --script res://tests/integration/anchor_rope_durability_scenarios.gd
godot --headless --path . --script res://tests/integration/rope_break_recovery_scenarios.gd
godot --headless --path . --script res://tests/unit/boarding_enemy_catalog_scenarios.gd
godot --headless --path . --script res://tests/integration/rope_saboteur_scenarios.gd
godot --headless --path . --script res://tests/integration/boarding_enemy_archetype_scenarios.gd
godot --headless --path . --script res://tests/integration/boarding_separation_scenarios.gd
godot --headless --path . --script res://tests/integration/boarding_jump_scenarios.gd
godot --headless --path . --script res://tests/integration/crew_command_panel_scenarios.gd
godot --headless --path . --script res://tests/integration/special_enemy_behavior_scenarios.gd
python tools/check_file_sizes.py
```

Godot-сценарии необходимо выполнить локально или подключить к CI перед слиянием. Текущий GitHub Actions может проверять только отдельные статические ограничения проекта.
