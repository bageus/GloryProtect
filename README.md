# GloryProtect

GloryProtect — survival roguelike с видом сбоку, в котором игрок управляет магически парящей платформой, поддерживает пять секций глобального защитного щита и защищает экипаж от физического абордажа.

## Движок

Проект разрабатывается на **Godot 4.6.2 stable** с использованием строго типизированного GDScript.

## Обязательные документы

Перед изменением кода или сцен необходимо прочитать:

- [Правила проекта](PROJECT_RULES.md)
- [Архитектура проекта](ARCHITECTURE.md)
- [Инструкции для coding agents](AGENTS.md)

Ключевые ограничения:

- запрещены монолитные классы, сцены и менеджеры;
- максимум 600 строк на поддерживаемый файл;
- с 450 строк файл должен оцениваться на разделение;
- каждое изменяемое состояние имеет одного владельца;
- настраиваемые значения находятся в ресурсах данных;
- UI не владеет игровой логикой.

## Основная идея

Платформа перемещается только по горизонтали и находится под воздействием ветра. Для восстановления секции щита игрок должен добраться до связанного наземного шара и удерживать энергетический контакт.

Якоря стабилизируют платформу, но каждый трос становится маршрутом физического абордажа. Ограниченный экипаж распределяется между управлением, якорями и боем.

## Текущая реализация

**Prototype 1.4** содержит:

- состояние забега, стартовую задержку и полную паузу;
- data-driven платформу и ветер силы 1–3;
- четыре декомпозированных якоря;
- пять фиксированных наземных шаров и секций щита;
- три физических защитника и перемещение между ролями;
- физический абордаж только через установленные якоря;
- ближний бой и независимые кулдауны замены;
- экономику забега и награды только за физических врагов;
- две одинаковые карточки-заглушки без эффектов;
- общую сложность забега;
- масштабирование физического и стратегического давления;
- агрегированные стратегические волны;
- поочерёдный урон от каждого стратегического врага;
- реальное объединение близких стратегических групп;
- случайное разделение крупных групп;
- перенаправление частей к другим секциям;
- плавные маршруты без телепортации после разделения;
- постоянно видимую стратегическую миникарту;
- регрессионные тесты.

## Сложность забега

Сложность растёт только в `RUNNING`. Стартовая задержка, ручная пауза, выбор карточек и завершённый забег не увеличивают её.

```text
Время до максимальной сложности: 600 секунд
Нормализованная сложность: 0.0 → 1.0
Наземный лимит: 8 → 20 врагов
Интервал физического спавна: 3.0 → 0.8 секунды
```

## Стратегические волны

Стратегические враги существуют как агрегированные группы данных и не создаются как физические `Node2D`.

```text
Первая волна: через 5 секунд активной игры
Интервал волн: 12 → 4 секунды
Размер волны: 6 → 30 врагов
Время движения: 8 → 4 секунды
Число атакуемых секций: 1 → 3
Урон одного врага: 1
Интервал ударов: 0.35 секунды
```

Одна волна выбирает уникальные секции и распределяет между ними своё реальное количество врагов. После прибытия группа остаётся у щита и уменьшается по одному врагу после каждого импульса урона.

## Объединение и разделение масс

Раз в `1.0` секунду система проверяет одну допустимую мутацию.

### Объединение

Две движущиеся группы могут объединиться, если они достаточно близки по углу и расстоянию до щита.

После объединения:

- остаётся одна группа;
- количество врагов суммируется;
- общее количество врагов не меняется;
- маршрут продолжается из усреднённой фактической позиции;
- сохраняется цель более крупной массы.

### Разделение

Крупная движущаяся группа может случайно разделиться на 2–3 части.

После разделения:

- сумма врагов во всех частях равна исходной группе;
- все части появляются в одной фактической точке;
- одна часть сохраняет прежнюю цель;
- другие части могут получить новые секции;
- каждая часть плавно строит новый маршрут;
- после мутации действует кулдаун `2.0` секунды.

Текущая вероятность разделения за проверку:

```text
Начало забега: 4%
Максимальная сложность: 22%
Минимальный размер группы: 10 врагов
Вероятность перенаправления части: 65%
```

Параметры находятся в:

```text
resources/balance/strategic_wave_balance.tres
```

## Стратегическая миникарта

Миникарта постоянно находится в правом нижнем углу и показывает:

- пять секций щита и их проценты;
- зелёное, оранжевое и мигающее красное состояние;
- фактическое положение стратегических групп;
- количество врагов внутри каждой массы;
- визуальное увеличение после объединения;
- расхождение частей после разделения;
- номер волны, время и размер следующей волны.

Миникарта получает только неизменяемые снимки и не меняет симуляцию.

## Экономика прототипа

```text
Стартовые монеты: 0
Награда за базового физического врага: 1
```

Награждаемые причины:

```text
combat
anchor_path_closed
```

Стратегические враги монет не дают.

## Временные карточки

Карточки открываются автоматически при накоплении нужной суммы.

```text
5, 10, 15, …, 100, 200, 400, 800
```

Обе карточки пока одинаковы и **не применяют улучшение**. Выбор только списывает стоимость и увеличивает номер покупки.

Выбор: мышь или клавиши `1` и `2`.

## Запуск

1. Установить Godot 4.6.2 stable.
2. Клонировать репозиторий.
3. Открыть `project.godot`.
4. Запустить проект клавишей `F5`.

## Управление

- `←` / `→` — рулевое усилие.
- `1–4` — установить или снять якорь.
- `R` — снять все якоря.
- `Esc` — пауза.
- `5`, `6`, `7` — выбрать защитника.
- `D` — рулевой.
- `Z` / `X` — левый / правый якорщик.
- `C` — свободный защитник.
- `F1–F5` — выбрать секцию щита.
- `Space` — тестовый урон секции.

## Тесты

```bash
godot --headless --path . --script res://tests/unit/strategic_wave_balance_scenarios.gd
godot --headless --path . --script res://tests/integration/strategic_wave_scenarios.gd
godot --headless --path . --script res://tests/integration/strategic_group_mutation_scenarios.gd
python tools/check_file_sizes.py
```

Полный список сценариев находится в [статусе реализации](docs/06_IMPLEMENTATION_STATUS.md).

## Документация

- [Правила проекта](PROJECT_RULES.md)
- [Архитектура проекта](ARCHITECTURE.md)
- [Обзор проекта](docs/00_PROJECT_OVERVIEW.md)
- [Game Design Document](docs/01_GAME_DESIGN_DOCUMENT.md)
- [Справочник игровых правил](docs/02_GAMEPLAY_RULES.md)
- [План разработки прототипа](docs/04_PROTOTYPE_ROADMAP.md)
- [Баланс и отложенные решения](docs/05_BALANCE_AND_OPEN_QUESTIONS.md)
- [Статус реализации](docs/06_IMPLEMENTATION_STATUS.md)

## Ключевой игровой выбор

> Насколько надёжно закрепить платформу, зная, что каждый дополнительный якорь открывает ещё один путь врагам?
