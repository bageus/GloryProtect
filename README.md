# GloryProtect

GloryProtect — survival roguelike с видом сбоку, в котором игрок управляет магически парящей платформой, поддерживает пять секций глобального щита и защищает экипаж от физического абордажа.

## Движок

Godot **4.6.2 stable**, строго типизированный GDScript.

## Обязательные документы

- [Правила проекта](PROJECT_RULES.md)
- [Архитектура проекта](ARCHITECTURE.md)
- [Инструкции для coding agents](AGENTS.md)

Основные ограничения:

- максимум 600 строк на поддерживаемый файл;
- каждое изменяемое состояние имеет одного владельца;
- баланс находится в типизированных ресурсах;
- UI не владеет игровой логикой.

## Текущая версия

**Prototype 1.8** содержит:

- горизонтальную платформу и ветер силы 1–3;
- четыре якоря с конечной длиной троса;
- пять наземных шаров и секций щита;
- три защитника, роли, замены и ближний бой;
- физический абордаж через якоря;
- стратегические волны и миникарту;
- экономику, статистику и итоговый экран;
- сетку размещаемых объектов;
- медицинский пост и роль лекаря;
- несколько независимых турелей;
- отдельного оператора для каждой турели;
- автоматическую стрельбу по ближайшим физическим врагам;
- визуальную подготовку выстрела, отдачу, вспышку и трассер;
- радиус выбранной турели;
- явное неактивное состояние турели без прибывшего оператора;
- две одинаковые карточки-заглушки без эффектов.

## Размещаемые объекты

`BuildableGrid` остаётся единственным владельцем занятости клеток и размещённых объектов.

- одна клетка содержит не больше одного объекта;
- служебные клетки недоступны;
- персонаж на клетке не мешает размещению;
- объекты не блокируют движение;
- открытие не теряется после демонтажа;
- медицинский пост существует максимум в одном экземпляре;
- в техническом прототипе можно открыть до четырёх турелей.

Координаты клеток вычисляет только `PlatformController`.

## Медицинский пост

Лекарь выбирает самого тяжело раненого живого защитника, при равенстве — ближайшего. После пяти секунд непрерывного лечения восстанавливается один сегмент. Начатый цикл завершается перед переназначением.

При переносе поста текущий цикл завершается, затем лекарь физически перебегает к новой позиции. При демонтаже он немедленно становится свободным бойцом.

## Турели

Боевые компоненты:

```text
TurretRuntime
TurretTargetSelector
TurretGeometry
TurretSystem
```

Компоненты представления:

```text
TurretVisualRuntime
TurretVisualController
TurretDebugInput
```

Каждая установленная турель:

- является отдельным динамическим постом роли `TURRET`;
- требует собственного защитника-оператора;
- без прибывшего оператора не работает;
- имеет круговой радиус действия;
- выбирает ближайшего живого врага;
- атакует врагов на тросах, платформе и в прыжке;
- наносит 1 урон;
- не перенаправляет начатый выстрел;
- работает независимо от других турелей.

Оператор турели не использует ближний бой. При новой команде он завершает текущий выстрел, затем уходит.

При переносе объект мгновенно меняет клетку. Если выстрел уже готовится, визуальная подготовка и трассер завершаются из старой позиции оператора. Новая турель остаётся неактивной до его прибытия.

При демонтаже выстрел отменяется, пост роли удаляется, оператор становится свободным бойцом, а открытие турели сохраняется.

## Визуальное состояние турелей

Выбранная турель показывает фактический круговой радиус.

Состояния:

- яркий корпус и зелёный индикатор — оператор прибыл;
- приглушённый корпус и красный индикатор — оператора нет или он в пути;
- растущий заряд у дула — подготовка выстрела;
- дуга вокруг башни — прогресс подготовки или кулдауна;
- короткая отдача, вспышка и трассер — завершённый выстрел.

Визуальный runtime не выбирает цели, не наносит урон и не меняет кулдаун.

## Баланс Prototype 1.8

```text
Максимум открытых турелей: 4
Урон: 1
Радиус: 360
Подготовка выстрела: 0.45 секунды
Кулдаун: 0.8 секунды
Вспышка: 0.10 секунды
Трассер: 0.14 секунды
```

Параметры находятся в:

```text
resources/balance/buildable_balance.tres
```

## Карточки

Стоимость:

```text
5, 10, 15, …, 100, 200, 400, 800
```

Обе карточки пока одинаковы и не применяют улучшение. Медицинский пост и турели открываются только debug-командами.

## Управление прототипом

- `←` / `→` — рулевое усилие.
- `1–4` — установить или снять якорь.
- `R` — снять все якоря.
- `Esc` — пауза.
- `5`, `6`, `7` — выбрать защитника.
- `D` — рулевой.
- `Z` / `X` — левый / правый якорщик.
- `C` — свободный защитник.
- `H` — назначить лекарем.
- `,` / `.` — выбрать клетку платформы.
- `B` — открыть медицинский пост.
- `M` — установить или перенести медицинский пост.
- `Delete` — демонтировать медицинский пост.
- `T` — открыть ещё одну турель.
- `G` — установить новую турель.
- `K` — выбрать следующую турель.
- `V` — перенести выбранную турель.
- `J` — назначить оператора выбранной турели.
- `Backspace` — демонтировать выбранную турель.
- `F1–F5` — выбрать секцию щита.
- `Space` — тестовый урон секции.
- `Enter` — новый забег на итоговом экране.

## Тесты Prototype 1.8

```bash
godot --headless --path . --script res://tests/integration/turret_scenarios.gd
godot --headless --path . --script res://tests/integration/turret_pause_scenarios.gd
godot --headless --path . --script res://tests/unit/turret_visual_runtime_scenarios.gd
godot --headless --path . --script res://tests/integration/buildable_medical_station_scenarios.gd
python tools/check_file_sizes.py
```

Полный список находится в [статусе реализации](docs/06_IMPLEMENTATION_STATUS.md).
