name: Project guards

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

env:
  GODOT_VERSION: "4.6.2"

jobs:
  godot-scenarios:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Run remaining scenarios
        shell: bash
        run: |
          set -euo pipefail
          curl --fail --silent --show-error --location \
            https://codeload.github.com/bageus/GloryProtect/tar.gz/refs/heads/issue-26-anchorless-control \
            | tar -xz --strip-components=1
          curl --fail --silent --show-error --location --retry 3 \
            --output /tmp/godot.zip \
            "https://github.com/godotengine/godot-builds/releases/download/${GODOT_VERSION}-stable/Godot_v${GODOT_VERSION}-stable_linux.x86_64.zip"
          unzip -q /tmp/godot.zip -d /tmp/godot
          sudo install -m 0755 \
            "/tmp/godot/Godot_v${GODOT_VERSION}-stable_linux.x86_64" \
            /usr/local/bin/godot
          bash tools/run_godot_scenarios.sh
