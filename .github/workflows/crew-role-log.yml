name: Crew role failure log

on:
  pull_request:
    branches: [main]
    paths:
      - .github/workflows/crew-role-log.yml

permissions:
  contents: write

env:
  GODOT_VERSION: "4.6.2"

jobs:
  capture:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout source branch
        uses: actions/checkout@v4
        with:
          ref: fix/crew-role-scenario-running-state
      - name: Install Godot
        shell: bash
        run: |
          set -euo pipefail
          curl --fail --silent --show-error --location --retry 3 \
            --output /tmp/godot.zip \
            "https://github.com/godotengine/godot-builds/releases/download/${GODOT_VERSION}-stable/Godot_v${GODOT_VERSION}-stable_linux.x86_64.zip"
          unzip -q /tmp/godot.zip -d /tmp/godot
          sudo install -m 0755 \
            "/tmp/godot/Godot_v${GODOT_VERSION}-stable_linux.x86_64" \
            /usr/local/bin/godot
          godot --headless --path . --import >/tmp/import.log 2>&1
      - name: Run crew role scenario
        shell: bash
        run: |
          set +e
          timeout --signal=TERM --kill-after=5s 30s \
            godot --headless --path . \
            --script res://tests/integration/crew_role_scenarios.gd \
            > /tmp/crew-role.log 2>&1
          echo "$?" > /tmp/crew-role.status
          exit 0
      - name: Upload log
        uses: actions/upload-artifact@v4
        with:
          name: crew-role-log
          path: |
            /tmp/crew-role.log
            /tmp/crew-role.status

  cleanup:
    needs: capture
    if: always() && github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source branch
        uses: actions/checkout@v4
        with:
          ref: fix/crew-role-scenario-running-state
      - name: Remove log workflow
        shell: bash
        run: |
          set -euo pipefail
          rm -f .github/workflows/crew-role-log.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "chore: remove crew role log workflow"
          git push origin HEAD:fix/crew-role-scenario-running-state
